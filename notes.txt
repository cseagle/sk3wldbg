resume mode step       no call to this for run
continue after event   (STEP, BREAKPOINT, ...)
insert step event into queue


when pausing, insert event into queue

after pause set resume mode to run

handle exceptions: UC_ERR_FETCH_UNMAPPED
